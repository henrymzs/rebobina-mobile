import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    input: {
        width: "100%",
        height: 52,
        borderRadius: 10,
        borderWidth: 1,
        padding: 12,
        fontSize: 16,
        backgroundColor: "#222222",
        color: "#FFFFFF"
    },
})